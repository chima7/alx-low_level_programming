#include <stdio.h>
/**
 * main - print alphabets except q and e
 *
 * Return: always (0)
 *
 */
int main(void)
{
	int n;
	int lastd;
	
	strand(time(0));
	n = rand() - RAND_MAX / 2;
	lastd = n % 10;
	if (lastd > 5)
	(
 	        print("last digit of %d is %d and is greater than s\n", n, lastd);
	)
	else if (lastd ==0)
	(
	        printf("last digit of %d is %d and is 0\n", n, lastd);
	)	
	else 
	(
	        printf("last digit of %d is %d and is less than 6and not o\n", n, lastd;)
	)	
	return (0);	

}
